<table>
            <thead>
                <tr>
                    <th>
                        <h2>タイトル</h2>
                    </th>
                    <th>
                        <h2>著者</h2>
                    </th>
                </tr>
            </thead>
            <tbody>
                {% for book in books.items %}
                <tr>
                    <td>
                        <img src="https://iss.ndl.go.jp/thumbnail/{{ book.isbn }}" onerror="this.onerror=null;this.src='static/images/gallery.png';"
                        />
                    </td>
                    <td>
                        <h3>{{ book.title }}</h3>
                    </td>
                    <td>
                        <h3>{{ book.creator }}</h3>
                    </td>
                    <td class="update">
                        <h3><a href="/{{book.id}}/update" role=" = button">編集</a></h3>
                    </td>
                    <td class="delete">
                        <h3><a href="/{{book.id}}/delete" role=" = button">削除</a></h3>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>